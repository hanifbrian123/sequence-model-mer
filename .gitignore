private/
.claude/
__pycache__/
cache/
.vscode/
models/
AGENTS.md

# Model weights: 132 MB per checkpoint, above GitHub's 100 MB hard limit.
# Re-evaluation snapshots stay local; the probs.npz artifacts they produce are
# committed instead, which is what every downstream comparison actually reads.
*.pt
runs/**/fold_checkpoints/
experiments/**/fold_checkpoints/


# Other Repository
fer/logs/
